<h1>Editing quiz</h1>

<%= form_for([@test, @quiz], url: test_quizzes_path(@test, @quiz), html: { method: :put } ) do |f| %>

	<%= render 'form', f: f %>

<% end %>

<%= link_to 'Show', @quiz %> |
<%= link_to 'Back', quizzes_path %>
