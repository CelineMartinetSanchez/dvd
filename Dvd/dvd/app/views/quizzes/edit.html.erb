<h1>Editing quiz</h1>

<%= form_for @quiz, url: quizzes_path(@quiz), html: { method: :put } ) do |f| %>

	<%= render 'form', f: f %>

<% end %>

<%= link_to 'Show', @quiz %> |
<%= link_to 'Back', quizzes_path %>
