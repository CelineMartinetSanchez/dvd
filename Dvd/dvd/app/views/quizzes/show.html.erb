<p id="notice"><%= notice %></p>

<table>
  <thead>
    <tr>
      <th>Questions</th>
      <th>Bonne réponse</th>
      <th>Réponse choisie</th>
      <th>Explication</th>
    </tr>
  </thead>

  <tbody>
    <% @quiz.questions.each do |q| %>
      <tr>
        <td><%= q.query %></td>
        <td><%= q.answers.first.good_answer %></td>
        <td><%= q.explanation %></td>
      </tr>
    <% end %>
  </tbody>
</table>



<%= link_to 'Edit', edit_quiz_path(@quiz) %> |
<%= link_to 'Back', quizzes_path %>
